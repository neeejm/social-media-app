spring:
  data:
    mongodb:
      # authentication-database: test
      # username: supergramAdmin
      # password: admin
      database: supergram
      # port: 27017
      # host: localhost

server:
  servlet:
    context-path: /api/v1

  port: 8081


spring.output.ansi.enabled: always